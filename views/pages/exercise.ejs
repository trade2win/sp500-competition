<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %>></title>
  </head>
  <body>
    <h1><%= title %></h1>
    Current second: <%= seconds %> <% if (seconds % 2 === 0) { %>
    <ul>
      <% for (item of items) { %>
      <li><%= item %></li>
      <% }%>
    </ul>
    <% } else { %>
    <p>No items to display</p>
    <% } %> <%- htmlContent %> <%- include('footer.ejs') %>
  </body>
</html>
